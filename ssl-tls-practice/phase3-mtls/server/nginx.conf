events {}
http {
    server {
        listen 443 ssl;
        server_name server;

        ssl_certificate     /etc/nginx/ssl/server.crt;
        ssl_certificate_key /etc/nginx/ssl/server.key;

        # mTLS: require clients to present a valid certificate
        ssl_client_certificate /etc/nginx/ssl/ca.crt;
        ssl_verify_client      on;

        location / {
            root /var/www/html;
            index index.html;
        }
    }
}
